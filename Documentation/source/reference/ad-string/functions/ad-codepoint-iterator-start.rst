ad_codepoint_iterator_start
===========================

.. c:function:: void ad_codepoint_iterator_start(AdCodepointIterator* it)

    Move the iterator to the start of the string range.

    :param it: the iterator

